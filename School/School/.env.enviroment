# rename the file name as .env, that should work
# modifiy the env variable accourding to your environvent

# local db enviroment setup
# ==================================
DB_ENGINE=django.db.backends.sqlite3
DB_NAME=db.sqlite3
DB_USER=your_sqlite_user
DB_PASSWORD=your_sqlite_password
DB_HOST=localhost
DB_PORT=5432

# dev server db enviroment setup
# ======================================
# ENGINE=django.db.backends.postgresql
# NAME=project101
# USER=tasim
# PASSWORD=tasim*#2024
# HOST=postgres_1
# PORT=5432